{
  "devDependencies": {
    "@tsconfig/recommended": "^1.0.1",
    "@types/chrome": "^0.0.180",
    "@types/node": "^17.0.24",
    "autoprefixer": "^10.4.4",
    "postcss": "^8.4.12",
    "postcss-cli": "^9.1.0",
    "tailwindcss": "^3.0.24",
    "vite": "^2.9.5",
    "bestzip": "^2.2.1"
  },
  "scripts": {
    "build": "tsc --noEmit && vite build && bestzip dist.zip dist/*",
    "dev": "vite build --watch"
  }
}
